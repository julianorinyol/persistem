<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="actions">
    <%= f.submit %>
  </div>
<p>title</p>
  <%= f.text_area :title, :class =>'note-inputs col-md-7'  %>
  <p>content</p>
  <%= f.text_area :content, :class =>'note-inputs col-md-7' %>
  <br>
<%= check_box 'note', 'public', {}, 'true', 'false' %>
  <%= label_tag(:public, "Share with Everyone") %>
 

<% end %>
<p>aasdfsdafdsbfkjshfksdhf</p>
<% if @note.title %>
  <p>delete that shit</p>

<%  end%>